{
  "name": "arucase",
  "version": "1.0.0",
  "description": "Arusha Catholic Seminary - School Management System",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "cd frontend && npm run lint",
    "test": "cd backend && npm test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "frontend/src/**/*.{js,jsx}": [
      "npx --prefix frontend eslint --fix --no-ignore"
    ]
  }
}
