/** Short labels for Template / Filled / Upload CSV toolbars */
export const CSV_BULK_LABELS = {
  template: 'Template',
  filled: 'Filled',
  upload: 'Upload',
  uploading: 'Uploading...',
};

/** Hover titles for CSV bulk buttons */
export const CSV_BULK_TITLES = {
  template: 'Download empty CSV template',
  filled: 'Download CSV with current data filled in',
  upload: 'Upload filled CSV file',
};
